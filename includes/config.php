<?php

  // This declares the name of the host along with its username and password, and also the name of the database to be accessed.
  define( 'DB_HOST', 'localhost' );
  define( 'DB_USER', 'root' );
  define( 'DB_PASS', '' );
  define( 'DB_NAME', 'SWLegion' );

  // This will allow ALL of the scripts to run with the Manila timezone.
  date_default_timezone_set('Asia/Manila');
?>
